{
  "name": "karven-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/redis.js && node --check src/auth.js && node --check src/schema.js && node --check src/migrate.js"
  },
  "engines": { "node": ">=22 <23" },
  "dependencies": {
    "@fastify/cors": "10.0.0",
    "@fastify/helmet": "13.0.0",
    "@fastify/rate-limit": "10.0.0",
    "fastify": "5.2.0",
    "ioredis": "5.4.1",
    "pg": "8.13.1",
    "zod": "3.24.1"
  }
}
